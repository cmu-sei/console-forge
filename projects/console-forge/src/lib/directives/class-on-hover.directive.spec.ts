import { ClassOnHoverDirective } from './class-on-hover.directive';

describe('ClassOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
