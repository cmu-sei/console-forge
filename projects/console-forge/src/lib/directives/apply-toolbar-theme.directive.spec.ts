import { ApplyToolbarThemeDirective } from '../src/directives/apply-toolbar-theme.directive';

describe('ApplyToolbarThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyToolbarThemeDirective();
    expect(directive).toBeTruthy();
  });
});
